[package]
name = "baker-dx"
version = "2.0.0-alpha.6"
authors = ["Chen Siyuan <chensiyuan-2009@outlook.com>"]
edition = "2024"
description = "《明日方舟：终末地》二创制作工具"
readme = "README.md"
license = "MIT"

[dependencies]
anyhow = "1.0.104"
dioxus = { version = "0.7.9", features = ["router"] }
dioxus-sdk = { version = "0.7.0", features = ["time"] }
fnv = "1.0.7"
futures = "0.3.33"
futures-util = "0.3.33"
indexed_db_futures = { version = "0.6.4", features = [
    "async-upgrade",
    "cursors",
    "indices",
    "serde",
    "streams",
    "version-change",
] }
markdown = "1.0.0"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
strum = { version = "0.28.0", features = ["derive"] }
strum_macros = "0.28.0"
uuid = { version = "1.23.2", features = ["js", "fast-rng", "v4", "serde"] }
wasm-bindgen-futures = "0.4.76"
web-sys = { version = "0.3.100", features = [
    "Storage",
    "Window",
    "File",
    "Blob",
    "Url",
] }

[build-dependencies]
heck = "0.5.0"
walkdir = "2.5"

[features]
default = ["web"]
# The feature that are only required for the web = ["dioxus/web"] build target should be optional and only enabled in the web = ["dioxus/web"] feature
web = ["dioxus/web"]
# The feature that are only required for the desktop = ["dioxus/desktop"] build target should be optional and only enabled in the desktop = ["dioxus/desktop"] feature
# desktop = ["dioxus/desktop"]
# The feature that are only required for the mobile = ["dioxus/mobile"] build target should be optional and only enabled in the mobile = ["dioxus/mobile"] feature
# mobile = ["dioxus/mobile"]
